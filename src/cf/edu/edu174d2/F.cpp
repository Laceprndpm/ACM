#include <bits/stdc++.h>
using namespace std;
using i64 = long long;
using PII = pair<int, int>;

class SegmentTree {
private:
    vector<int> value;
    int         n;

public:
    void 

}

void solve()

{
    int n, q;
    cin >> n >> q;
    while (q--) {
    }
}

signed main(int argc, char** argv)
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    solve();
    return 0;
}