#include <bits/stdc++.h>
using namespace std;
using LL  = long long;
using PII = pair<int, int>;

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int p;
    cin >> p;
    cout << (int)p * log10(2);
    return 0;
}